name: learningdart
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  path: any
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^2.16.0 # Core Firebase integration
  firebase_auth: ^4.8.0 # Firebase Authentication

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
